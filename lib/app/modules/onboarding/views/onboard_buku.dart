import 'package:flutter/material.dart';

class OnboardBukuView extends StatelessWidget {
  const OnboardBukuView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Onboard Buku (placeholder)')),
    );
  }
}
